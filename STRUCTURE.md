HC42规格在 docs

DSP流程（自动量程，每个测量周期跑一遍）

探测帧   ADC1.3 直采 PA18，X1，160 点/40 ms      -> mean, pp（输入端 LSB）
选档     最大的档使 G*pp/2 <= 65% 可用余量        -> G ∈ {1,2,4,8,16,32}
定偏置   Vdac = (C - G*mean)/(1 - G)，C = 2047   -> 输出直流落回窗口中心
主帧     4k sps，800 点 = 10 个 50Hz 周期        -> ADC1.1(OPA1_OUT) 或 ADC1.3(x1)
RMS      Hann 加权，减本帧算出的均值             -> f32 LSB
标定     按档查对应的 CAL 表，分段线性           -> f32 A <output>

所有档共用一个窗口 0-4095（见 range.rs 的 OUT_LO/OUT_HI）。这里只有一个限制：ADC 的
基准。ADC1 用内部 2.5 V VREF（VRSEL=2），满量程 0-4095 就是 0-2.5 V；OPA1 仍由
VDDA/VSSA 供电、能摆到 3.3 V，所以卡住量程的是转换器而不是放大器。

这样反而更干净：超过 2.5 V 码字就钉在 4095，没有软肩要建模，而放大器还在线性区、不
额外贡献压缩。代价是超量程的输入在样本里只表现为一条平顶——probe_stats 的撞轨标志就
是为了抓这个，也是它必须在选档算术之前检查的原因。

DAC12 必须和 ADC 共用这个基准（CTL1.REFSP=VREF+）。range.rs 的 dac_code_for 直接用
ADC 码解出 DAC 码，两个 12 位转换器共用一个基准是这段算术唯一的前提；把任何一个挪到
别的基准上，所有偏置码都会按两者之比悄悄错掉。

硬件前提两条，固件查不了。CVREF 那条 LP 板自带（C8 = 1 µF 接在 VREF+ 与 VREF- 之
间，R3 = 0 Ω 把 VREF- 接地），换自制板要照抄，数据手册 7.15.2 注 5 说没有 CVREF 就
不得使能这个模块。另一条没人替你保证：2.5 V 档要求 VDD ≥ 2.7 V，取电轨掉到这以下时
基准已经超规，而 READY 位仍然读 1——RefBad 抓不到它。

复测用 src/bin/oparails.rs（DAC 当激励扫 OPA1，两端各用能真正把它压饱和的接法，每
个读数都带命令值）。换供电或换板子时值得重跑，运行时不依赖它。

x1 之所以必须是一个真档而不是特例：阶梯最低就是 2x，而 2x 把直流也放大 2 倍
（Vout <= 2*Vin_dc，DAC 改不了），输入直流偏低时 PGA 各档都够不着量程顶端，只有
x1 装得下。

输入直流不作任何假设：由探测帧实测，DAC 跟着走。探测帧撞到 ADC 两端、或连 x1 都
装不下（输入自身越出 0..VDDA）时，读数前会打标记，不静默出数。

外部电路的两根控制线是一对互补信号，测量态 PA8 高 + PA26 低，空闲态反过来，只成对
翻转。两根都需要外部电阻定住空闲电平（PA8 下拉、PA26 上拉）——上电到固件配置引脚
之间它们是输入不是输出，这段窗口覆盖整个启动过程。

一次测量之外不留模拟负载：主帧一结束就按下游到上游的顺序清 PWREN，TIMG6/ADC1 ->
OPA1/DAC12 -> 外部电路回空闲态。PA18 的 IOMUX 从上电起就是高阻（PINCM 整个写 0，无功能、无输
入缓冲、无上下拉），但真正压住这个脚的是模拟侧——OPA1 使能时 PSEL=EXTPIN1 会把它
接到放大器输入并抽偏置电流，ADC1 上电时它也还挂在通道 mux 上，所以只有掉电才是断
开。空闲时这个脚上只剩焊盘漏电。下一次按键在 AFE_SETTLE_MS 的窗口里把四块重新完
整初始化（PWREN 一清寄存器就复位，没有"恢复"这回事），增益和偏置从上一周期的结果
起步。

两个固件共用 `meter::measure` 这一次完整测量（AFE 换态、上电、探测、选档、主帧、
掉电、RMS、查表），所以一张 CAL 表在两边都成立：

- `main` 上电什么都不干，先等电源轨到 `BOOT_MIN_MV`（3300 mV）。`supply.rs` 用
  ADC0 的 15 号通道读 VDD/3（这一路不占引脚，选中它就把外部输入断开、接到片内分
  压上），每 200 ms 一次，**没有超时**——到不了 3.3 V 就一直暗着等在那儿。等待期间
  没有任何模拟块使能、射频没上电、指示灯也不点亮，所以取电轨不用一边充一边被抽。

  **每次读之前必须等基准建立**（`VREF_SETTLE_MS`，20 ms，和 `meter::measure` 给它
  的那段一样）。码字是 VDD/3 与 VREF 的**比值**，`vref::init` 返回的 READY 只说基准
  内核在跑，不说那颗 1 µF 的 CVREF 充到 2.5 V 了——上电时它是空的，而基准偏低会让
  供电**读高**，正好是门该守住的那个方向。另外码字换算出 3.6 V 以上直接判无效
  （`MAX_PLAUSIBLE_CODE`）：芯片在这个电压之上根本不执行，那种码字讲的是基准不是轨，
  而基准塌到底会报出 7.5 V，比谁在这儿写的门限都高。
  基准就用测量链那 2.5 V，全程只有这一个基准电压，谁都不写 BUFCONFIG。VDD/3 在
  3.3 V 时是 1.1 V，落在 code 1802 / 4095，1 code = 1.83 mV；满量程 7.5 V 远高于
  3.6 V 上限，任何芯片活得下来的电压都翻不到量程顶上再装成好轨。2.5 V 档要求
  VDD ≥ 2.7 V，比要判的那条线**低**，所以判决点本身在规格内；低于 2.7 V 时基准跟
  着供电往下掉，码字只会读**高**不会读低，要伪造一次 3.3 V 通过得让基准塌到
  1.97 V，那个供电下 CPU 早就不执行了。超规在这里只意味着不准，不意味着判反。
  1.4 V 档确实到 1.62 V 都成立，但换它就意味着两个基准电压来回：CVREF 那颗 1 µF
  被充到 2.5 V 之后 1.4 V 缓冲器拉不下来，TRM 23.2.1 要先关模块、把 PA23 当 GPIO
  拉低 100 µs 放电。一道 3.3 V 的门不需要 2 V 处的有效读数，只需要 3.3 V 处的正确
  是/否，一个基准白给。

  等待是轮询不是硬件盯着。ADC0 的窗口比较器能把 HIGHIFG 发到事件织物上自动盯，但
  更费电：VREF 只有 CPU 能开关，一旦让转换自动跑，基准就得常开，189 µA typ /
  330 µA max；而轮询把它压到 10% 占空比，合 19 µA。每次看一眼几乎全是那 20 ms 的
  基准建立，CPU 在里面是睡着的；自动盯着的方案照样得为这段建立时间常年买单，读的
  还是同一个转换器。BOR 也顶不上，可编程门限最高 BOR3+ 才 3.04 V。

  门开了才是原来的流程：先自己走一遍完整采集（这一步仍在射频上电之前，按板子文档
  6.1 的顺序，不让模块启动电流和取电轨抢；读数攥在手里，端口一开就发出去）。之后
  出数的触发有两个：按 S2，或蓝牙收到 `MEAS`。两者完全等价，和上电那一次也走同一
  条路径；测量期间再来的请求两边都丢弃不排队（按键靠 `wait_for_falling_edge` 清掉
  待处理边沿，蓝牙靠 `Link::discard_pending` 清掉缓冲字节）。出数后先发帧再刷屏，
  显示 `DISPLAY_ON_MS` 后熄屏。蓝牙常驻上电，因为掉电的模块没法被远端唤醒。
  `ui.rs` 自己不查电源——能走到刷屏这一步，轨早就量过了。
- `stream` 定时自测自发，每 `PERIOD_MS` 一次，测量之外模拟部分照样全掉电；蓝牙
  一旦上电就不再断。每次发两行：`METER_TEST`（安卓端解析的那条，正则锚到行尾，
  不能往后加字段）和 `METER_CAL`（原始 RMS、档位、探测帧的 mean/pp、质量标志，
  手机忽略，`cargo xtask cal-log` 收）。

两个固件的无线部分都走 `link.rs`，所以线上语法只有一份。命令方向是
`MEAS`（广播）或 `MEAS,ADDR=n`（只有地址开关读数等于 n 的表响应）。故意不用
`METER_*` 打头——命令和上报共用同一条透传链路，一个电流表认得出自己输出的语法，
就离"被自己的回显触发"只差一次回环。

现场标定（`nvcal.rs` + `cal.rs` 的 `Table` + 安卓「C表标定台」）

1(1) 那 30 分比的是电流表**自己显示**的读数对标定表，而 `CAL_X1` 是在自己台子上、
对着自己那台参考表拟的。现场换一台 4½ 位手持表，AC 档的差就可能盖过 0.5% 的整个
预算。所以现场标定改的是表里的表，不是手机上的显示。

- 线上四条命令，都必须带 `ADDR`（标定表属于量它的那台表，没有广播形式）：
  `CALPT,ADDR=n,I=k,X=<LSB>,Y=<A>` 暂存一个点、`CALEND,ADDR=n,N=m` 校验并装表、
  `CALOFF,ADDR=n` 退回出厂表、`CALGET,ADDR=n` 问现在用的是哪张。应答是
  `CALACK,ADDR=n,I=k[,ERR=…]` 和 `CALSTAT,ADDR=n,SRC=FIELD|ROM,N=m[,ERR=…]`。
- 点先进暂存区、`CALEND` 才装表：推送在线上不是原子的，一条 `CALPT` 掉了也不能让
  表用半张表测。不合格整张拒收，退回出厂表——而那件事它会在每一帧里说出来。
- `CALEND` 里写 flash（MAIN 最后一个 1 KB 扇区，`memory.x` 已经把链接器挡在它外面）。
  记录是**追加**的：一条 144 字节，一扇区放 7 条，所以七次里只有一次要擦；magic 字
  最后写，掉电中断的那次留下的是一条 `load` 直接跳过的槽，不是半张它会信的表。读走
  未纠错别名 0x0040_0000，因为掉电写坏的字在纠错通路上是双位错、等于一次 NMI。
- 每一帧 `METER_CAL` 带 `SRC=FIELD|ROM`。这是防"静默失效"的那一道：表靠被测电流
  取电，回路一断就掉电，flash 没写成或读不回来时它会退回出厂表，而那件事表现出来
  只是"读数差了一点"。标定台看到 ROM 会自动重推。
- 装表只要求 RMS 那一列严格递增——`lsb_to_amps` 靠它找落在哪一段，不单调就没有
  定义。电流那一列不管：它可以上上下下，插值照样成立。标定台不拦记点（稳没稳、
  FLAG 是什么、点挨多近、什么顺序记，都随操作的人），只在推送时排一次序、把 RMS
  重复的合并成后记的那个。

output 1. OLED 128x64 显示
output 2. HC-42 串口透传为蓝牙
output 3. PA0 板载红色 LED1，轨到 3.3 V 起 3 Hz 闪烁，表示轨已就位且固件在跑
（OLED 除了出数那一秒都是暗的，暗屏分不出还在充电、卡住、还是在待机）

安卓端
BLE 连接 HC-42，（1）读取距离≥5m。（3 分）
（2）读取数据功能：基本模式和自动模式读取电流表读数。基本模式：即一对一读
取，不操作电流表，但允许操作读表器，手动读取；自动方式：能自动搜索其无
线覆盖范围内的电流表，每 2 分钟自动采集一次，读表器一键启动，电流表除
地址码设置外，不得操作其他功能键。（10 分）
（3）报警指示功能：负载电流小于 0.2A 时低限报警，大于 2A 超限报警，负载断
开后离线指示。 （5 分）
（4）具有数据储存、查看历史数据、显示电流变化趋势曲线的能力；数据记录中包
括读取时间、电流表编号（或地址）、电流值、是否超限等信息，至少能存储30
条信息以上。具有数据掉电保护的功能。（8 分）
