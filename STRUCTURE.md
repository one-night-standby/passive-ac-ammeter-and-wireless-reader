题面/HC42规格在 docs

DSP流程（自动量程，每个测量周期跑一遍）

探测帧   ADC1.3 直采 PA18，X1，160 点/40 ms      -> mean, pp（输入端 LSB）
选档     最大的档使 G*pp/2 <= 65% 可用余量        -> G ∈ {1,2,4,8,16,32}
定偏置   Vdac = (C - G*mean)/(1 - G)，C = 2047   -> 输出直流落回窗口中心
主帧     4k sps，800 点 = 10 个 50Hz 周期        -> ADC1.1(OPA1_OUT) 或 ADC1.3(x1)
RMS      Hann 加权，减本帧算出的均值             -> f32 LSB
标定     按档查对应的 CAL 表，分段线性           -> f32 A <output>

所有档共用一个窗口 0-4095（见 range.rs 的 OUT_LO/OUT_HI）。这里只有一个限制：ADC 的
基准。ADC1 用内部 2.5 V VREF（VRSEL=2），满量程 0-4095 就是 0-2.5 V；OPA1 仍由
VDDA/VSSA 供电、能摆到 3.3 V，所以卡住量程的是转换器而不是放大器。

这样反而更干净：超过 2.5 V 码字就钉在 4095，没有软肩要建模，而放大器还在线性区、不
额外贡献压缩。代价是超量程的输入在样本里只表现为一条平顶——probe_stats 的撞轨标志就
是为了抓这个，也是它必须在选档算术之前检查的原因。

DAC12 必须和 ADC 共用这个基准（CTL1.REFSP=VREF+）。range.rs 的 dac_code_for 直接用
ADC 码解出 DAC 码，两个 12 位转换器共用一个基准是这段算术唯一的前提；把任何一个挪到
别的基准上，所有偏置码都会按两者之比悄悄错掉。

硬件前提两条，固件查不了。CVREF 那条 LP 板自带（C8 = 1 µF 接在 VREF+ 与 VREF- 之
间，R3 = 0 Ω 把 VREF- 接地），换自制板要照抄，数据手册 7.15.2 注 5 说没有 CVREF 就
不得使能这个模块。另一条没人替你保证：2.5 V 档要求 VDD ≥ 2.7 V，取电轨掉到这以下时
基准已经超规，而 READY 位仍然读 1——RefBad 抓不到它。

复测用 src/bin/oparails.rs（DAC 当激励扫 OPA1，两端各用能真正把它压饱和的接法，每
个读数都带命令值）。换供电或换板子时值得重跑，运行时不依赖它。

x1 之所以必须是一个真档而不是特例：阶梯最低就是 2x，而 2x 把直流也放大 2 倍
（Vout <= 2*Vin_dc，DAC 改不了），输入直流偏低时 PGA 各档都够不着量程顶端，只有
x1 装得下。

输入直流不作任何假设：由探测帧实测，DAC 跟着走。探测帧撞到 ADC 两端、或连 x1 都
装不下（输入自身越出 0..VDDA）时，读数前会打标记，不静默出数。

外部电路的两根控制线是一对互补信号，测量态 PA8 高 + PA26 低，空闲态反过来，只成对
翻转。两根都需要外部电阻定住空闲电平（PA8 下拉、PA26 上拉）——上电到固件配置引脚
之间它们是输入不是输出，这段窗口覆盖整个启动过程。

一次测量之外不留模拟负载：主帧一结束就按下游到上游的顺序清 PWREN，TIMG6/ADC1 ->
OPA1/DAC12 -> 外部电路回空闲态。PA18 的 IOMUX 从上电起就是高阻（PINCM 整个写 0，无功能、无输
入缓冲、无上下拉），但真正压住这个脚的是模拟侧——OPA1 使能时 PSEL=EXTPIN1 会把它
接到放大器输入并抽偏置电流，ADC1 上电时它也还挂在通道 mux 上，所以只有掉电才是断
开。空闲时这个脚上只剩焊盘漏电。下一次按键在 AFE_SETTLE_MS 的窗口里把四块重新完
整初始化（PWREN 一清寄存器就复位，没有"恢复"这回事），增益和偏置从上一周期的结果
起步。

两个固件共用 `meter::measure` 这一次完整测量（AFE 换态、上电、探测、选档、主帧、
掉电、RMS、查表），所以一张 CAL 表在两边都成立：

- `main` 上电先自己走一遍完整采集（这一步在射频上电之前，按板子文档 6.1 的顺序，
  不让模块启动电流和刚建立的取电轨抢；读数攥在手里，端口一开就发出去）。之后出数
  的触发有两个：按 S2，或蓝牙收到 `MEAS`。两者完全等价，和上电那一次也走同一条
  路径；测量期间再来的请求两边都丢弃不排队（按键靠 `wait_for_falling_edge` 清掉
  待处理边沿，蓝牙靠 `Link::discard_pending` 清掉缓冲字节）。出数后先发帧再刷屏，
  显示 `DISPLAY_ON_MS` 后熄屏。蓝牙常驻上电，因为掉电的模块没法被远端唤醒。

  OLED 的第一次点亮要等电源轨。`supply.rs` 用 ADC0 的 15 号通道读 VDD/3（这一路
  不占引脚，选中它就把外部输入断开、接到片内分压上），基准取 1.4 V 而不是测量链
  那 2.5 V——2.5 V 档 VDD 低于 2.7 V 就超规而 READY 照样读 1，正好废在要看的那
  一段；1.4 V 档 1.62 V 以上都成立，满量程折算回去是 4.2 V，盖住整个供电范围。
  读数不到 `OLED_MIN_MV`（3300 mV）就完全不碰屏，连一次 I2C 事务都不发，也不消
  耗 `OLED_INIT_ATTEMPTS` 里的重试次数，而是每 20 ms 复测一次，**没有超时**——
  到不了 3.3 V 就一直等在那儿。第一次问到这道门的时刻正是上电那次不请自来的采集
  之后，取电轨刚建立；设个期限的话，到期只有两种收场，要么在实测偏低的轨上硬点
  屏，要么放弃返回、屏一直暗到有人按 S2，那是在等操作员不是在等电源。所以按
  `main` 里 `link.send` 在 `panel.show` 之前的顺序：手上这一帧早发出去了，等待
  压住的是它之后的东西——心跳停发、`MEAS` 不应答，直到轨上来。这道门只管"开始
  驱动"：屏一旦起来就不再查电源，轨后来掉下去也不把已经点亮的屏收回。
- `stream` 定时自测自发，每 `PERIOD_MS` 一次，测量之外模拟部分照样全掉电；蓝牙
  一旦上电就不再断。每次发两行：`METER_TEST`（安卓端解析的那条，正则锚到行尾，
  不能往后加字段）和 `METER_CAL`（原始 RMS、档位、探测帧的 mean/pp、质量标志，
  手机忽略，`tools/cal_log.py` 收）。

两个固件的无线部分都走 `link.rs`，所以线上语法只有一份。命令方向是
`MEAS`（广播）或 `MEAS,ADDR=n`（只有地址开关读数等于 n 的表响应）。故意不用
`METER_*` 打头——命令和上报共用同一条透传链路，一个电流表认得出自己输出的语法，
就离"被自己的回显触发"只差一次回环。

output 1. OLED 128x64 显示
output 2. HC-42 串口透传为蓝牙
output 3. PA0 板载红色 LED1，上电起 3 Hz 闪烁，只表示固件在跑（OLED 除了出数
那一秒都是暗的，暗屏分不出没上电、卡住、还是在待机）

安卓端
BLE 连接 HC-42，（1）读取距离≥5m。（3 分）
（2）读取数据功能：基本模式和自动模式读取电流表读数。基本模式：即一对一读
取，不操作电流表，但允许操作读表器，手动读取；自动方式：能自动搜索其无
线覆盖范围内的电流表，每 2 分钟自动采集一次，读表器一键启动，电流表除
地址码设置外，不得操作其他功能键。（10 分）
（3）报警指示功能：负载电流小于 0.2A 时低限报警，大于 2A 超限报警，负载断
开后离线指示。 （5 分）
（4）具有数据储存、查看历史数据、显示电流变化趋势曲线的能力；数据记录中包
括读取时间、电流表编号（或地址）、电流值、是否超限等信息，至少能存储30
条信息以上。具有数据掉电保护的功能。（8 分）
