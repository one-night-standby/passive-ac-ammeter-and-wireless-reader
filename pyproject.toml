[project]
name = "passive-ac-ammeter-and-wireless-reader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ozongzi", email = "ozongzi@icloud.com" }
]
requires-python = ">=3.14"
dependencies = [
    "bleak>=3.0.2",
    "pyusb>=1.3.1",
    "pyvisa>=1.16.2",
    "pyvisa-py>=0.8.1",
]

[project.scripts]
passive-ac-ammeter-and-wireless-reader = "passive_ac_ammeter_and_wireless_reader:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
